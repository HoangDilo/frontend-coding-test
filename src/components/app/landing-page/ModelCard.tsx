import { IItemModel } from '@/types/model.type';
import * as React from 'react';

export interface IModelCardProps {
    item: IItemModel
}

export default function App ({item}: IModelCardProps) {
  return (
    <div className=''>
      
    </div>
  );
}
