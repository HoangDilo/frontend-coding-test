import * as React from 'react';

export interface IStoryCardProps {
    title: string;
    content: string;
    img_url: string;
}

export default function StoryCard ({}: IStoryCardProps) {
  return (
    <div>
      
    </div>
  );
}
